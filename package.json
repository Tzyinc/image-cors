{
  "name": "image-cache-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "In-memory image proxy with cached filters and on-demand scaling.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "sharp": "^0.35.0"
  }
}
